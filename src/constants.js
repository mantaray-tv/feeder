/* eslint-disable prefer-const */
export let MAX_NUM_PROXY = 25
export let MAX_BYTES_SELECT_PER_TORRENT = 1024 * 1024 * 1024
export let COORDINATOR_URL = 'https://coordinator.mantaray.tv/listen'

export default {
  MAX_NUM_PROXY,
  MAX_BYTES_SELECT_PER_TORRENT,
  COORDINATOR_URL
}
